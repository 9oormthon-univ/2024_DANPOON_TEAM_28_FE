import Header from '@/components/Header';
import Layout from '@/layouts/Layout';

const MissionPage = () => {
    return (
        <Layout>
            <Header></Header>
        </Layout>
    );
};

export default MissionPage;
